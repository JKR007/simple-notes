<%= simple_form_for note, html: { class: "note form" } do |f| %>
  <%= form_error_messages(note) %>

  <%= f.input :title, error: false, input_html: { autofocus: true } %>
  <%=link_to "Cancel", notes_path, class: 'btn btn--light' %>
  <%= f.submit class: "btn btn--secondary" %>
<% end %>
