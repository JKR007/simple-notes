<main>
  <% link_to sanitize("&larr; Back to Notes"), notes_path %>
  <div class="header">
    <h1>New Note</h1>
  </div>

  <%= turbo_frame_tag @note do %>
    <%= render 'form', note: @note %>
  <% end %>
</main>
